{
  "scripts": {
    "start": "gulp build && electron build",
    "build": "gulp build",
    "run": "electron build"
  },
  "devDependencies": {
    "electron-prebuilt": "^0.36.0",
    "fs-jetpack": "^0.7.0",
    "gulp": "^3.9.0",
    "gulp-inject": "^3.0.0",
    "gulp-less": "^3.0.5",
    "gulp-sourcemaps": "^1.6.0",
    "gulp-typescript": "^2.10.0",
    "typescript": "^1.7.3"
  }
}
